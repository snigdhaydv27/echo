name: Keep Render Alive

on:
  schedule:
    - cron: '*/10 * * * *' # Runs every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send HTTP request to backend
        run: |
          curl -f https://echo-backend-6n38.onrender.com/ || exit 0